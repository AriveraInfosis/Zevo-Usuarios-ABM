# Conexion a SQL Server (ToolsDB)
DB_SERVER=localhost\SQLEXPRESS
DB_DATABASE=ToolsDB
DB_DRIVER=ODBC Driver 17 for SQL Server
DB_TRUSTED_CONNECTION=True
DB_USER=
DB_PASSWORD=

# JWT
JWT_SECRET_KEY=cambiar-esto-por-una-clave-larga-y-random
JWT_ALGORITHM=HS256
JWT_EXPIRE_MINUTES=480

# Reglas de negocio
ALLOWED_EMAIL_DOMAIN=@infosis.tech

# CORS (frontend React en local, Vite default port)
CORS_ORIGINS=["http://localhost:5173"]
